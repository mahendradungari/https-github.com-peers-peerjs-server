{
  "name": "peerjs-server",
  "version": "1.0.0",
  "description": "PeerJS signaling server for WebRTC",
  "main": "server.js",
  "scripts": {
    "start": "peerjs --port 9000 --path /peerjs"
  },
  "dependencies": {
    "peer": "^0.6.1",
    "express": "^4.18.2"
  }
}
